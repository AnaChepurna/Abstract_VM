#include "VirtualMachine.h"

int main(int argc, char **argv)
{
	VirtualMachine virtualMachine;

//	if (argc > 2)
//		return (0);
//	if (argc == 2)
//		virtualMachine = VirtualMachine(argv[1]);
//	else
//		virtualMachine = VirtualMachine();
	std::cout << "ready\n";
//	virtualMachine.run();
	return (0);
}